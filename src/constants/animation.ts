// Centralized animation configuration
// Change this value to adjust all animation speeds across the app

export const ANIMATION_DURATION = 500; // milliseconds

// Tailwind CSS class for transitions (must match ANIMATION_DURATION)
// Note: If you change ANIMATION_DURATION, update this to match
// 300 -> duration-300, 500 -> duration-500, 700 -> duration-700, 1000 -> duration-1000
export const ANIMATION_DURATION_CLASS = 'duration-500';
